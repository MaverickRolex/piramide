<tr>
  <th scope="row" class="align-middle text-center"><%= payment&.id %></th>
  <td class="align-middle"><%= payment&.created_at %></td>
  <td class="align-middle text-end">$ <%= payment&.amount %>.00</td>
    <td class="align-middle text-end">
      <%= button_to payment_path(payment), method: :delete, class: "btn btn-danger btn-sm" do %>
        <i class="bi bi-x-lg"></i>
      <% end %>
    </td>
</tr>